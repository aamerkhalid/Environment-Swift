# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Environment' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Environment
pod 'Firebase/Core'
pod 'Firebase/Firestore'
end

target 'Environment Dev' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Environment Dev
pod 'Firebase/Core'
pod 'Firebase/Firestore'
end

target 'Environment QA' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Environment QA
pod 'Firebase/Core'
pod 'Firebase/Firestore'
end
